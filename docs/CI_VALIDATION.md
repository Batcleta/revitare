# CI Validation

Temporary pull-request marker used to validate the initial Next.js MVP against the repository CI workflow.

The permanent CI workflow lives at `.github/workflows/ci.yml` and runs:

- clean dependency installation;
- TypeScript validation;
- production Next.js build.
